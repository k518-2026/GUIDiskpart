<Window x:Class="GUIDiskpart.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:GUIDiskpart" 
        Title="{DynamicResource AppTitle}" Height="600" Width="780"
        SizeToContent="WidthAndHeight" 
        Loaded="Window_Loaded"
        Background="#FFF4F4F6">

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3.2*" />
            <ColumnDefinition Width="2.2*" />
        </Grid.ColumnDefinitions>

        <!-- 左カラム：更新ボタン ＋ ディスクツリー -->
        <Grid Grid.Column="0" Margin="0,0,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Button x:Name="btnRefresh" Grid.Row="0" Content="{DynamicResource BtnRefresh}" 
                    Margin="0,0,0,10" Padding="10" FontWeight="Bold" Background="#FFE1E6EB" Cursor="Hand" Click="btnRefresh_Click"/>

            <TreeView x:Name="treeDisks" Grid.Row="1" SelectedItemChanged="treeDisks_SelectedItemChanged" BorderBrush="#FFCCD1D9">
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsExpanded" Value="True" />
                        <Setter Property="Padding" Value="4"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsDangerous}" Value="True">
                                <Setter Property="Background" Value="#FFFFD6D6" />
                                <Setter Property="Foreground" Value="#FF900000" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsInternal}" Value="True">
                                <Setter Property="Background" Value="#FFFFF3D6" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TreeView.ItemContainerStyle>

                <TreeView.Resources>
                    <DataTemplate x:Key="PartitionTemplate">
                        <StackPanel Orientation="Horizontal" Margin="2">
                            <TextBlock Text="└ Partition " Foreground="#FF7A8288"/>
                            <TextBlock Text="{Binding PartitionNumber}" FontWeight="Bold" Margin="0,0,6,0" />
                            <TextBlock Text="{Binding DriveLetterString}" Foreground="#FF0056B3" FontWeight="Bold" Margin="0,0,6,0" />
                            <TextBlock Text="{Binding SizeGB}" Margin="0,0,4,0" />
                            <TextBlock Text="GB (" Foreground="#FF7A8288"/>
                            <TextBlock Text="{Binding Type}" Foreground="#FF7A8288" />
                            <TextBlock Text=")" Foreground="#FF7A8288" />
                        </StackPanel>
                    </DataTemplate>

                    <HierarchicalDataTemplate DataType="{x:Type local:DiskInfo}" 
                                              ItemsSource="{Binding Partitions}" 
                                              ItemTemplate="{StaticResource PartitionTemplate}">
                        <StackPanel Orientation="Horizontal" Margin="2,4">
                            <TextBlock Text="💾 Disk " FontWeight="Bold" />
                            <TextBlock Text="{Binding Number}" FontWeight="Bold" Margin="0,0,8,0" />
                            <TextBlock Text="{Binding FriendlyName}" Margin="0,0,8,0" />
                            <TextBlock Text="{Binding SizeGB}" Margin="0,0,4,0" />
                            <TextBlock Text="GB [" Foreground="#FF7A8288"/>
                            <TextBlock Text="{Binding BusType}" Foreground="#FF7A8288"/>
                            <TextBlock Text="]" Foreground="#FF7A8288"/>
                            <TextBlock Text="{Binding WarningLabel}" Foreground="#FFD9534F" FontWeight="Bold" Margin="8,0,0,0"/>
                        </StackPanel>
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>
        </Grid>

        <!-- 右カラム：操作パネル -->
        <StackPanel Grid.Column="1" x:Name="OperationPanel">

            <!-- 言語切り替えボタンを右上に配置 -->
            <DockPanel LastChildFill="False" Margin="0,0,0,8">
                <Button x:Name="btnLang" Content="{DynamicResource LangButton}" Width="80" Height="26" DockPanel.Dock="Right" Click="btnLang_Click" Cursor="Hand"/>
            </DockPanel>

            <Border Background="White" BorderBrush="#FFCCD1D9" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,12">
                <TextBlock x:Name="txtSelectedTarget" Text="Selected: None" FontWeight="Bold" TextWrapping="Wrap" Foreground="#FF333333"/>
            </Border>

            <GroupBox Header="{DynamicResource GrpDiskOperation}" Margin="0,0,0,10" FontWeight="Bold">
                <StackPanel Margin="5">
                    <Button x:Name="btnClean" Content="{DynamicResource BtnClean}" Margin="0,4" Padding="8" Background="#FFFFD6D6" Foreground="#FF900000" Cursor="Hand" Click="btnClean_Click" IsEnabled="False" />
                    <Button x:Name="btnPartition" Content="{DynamicResource BtnPartition}" Margin="0,4" Padding="8" Cursor="Hand" Click="btnPartition_Click" IsEnabled="False" />
                    <Button x:Name="btnEject" Content="{DynamicResource BtnEject}" Margin="0,4" Padding="8" Background="#FFE6F7FF" Foreground="#FF0056B3" Cursor="Hand" Click="btnEject_Click" IsEnabled="False" />
                </StackPanel>
            </GroupBox>

            <GroupBox Header="{DynamicResource GrpPartOperation}" Margin="0,0,0,10" FontWeight="Bold">
                <StackPanel Margin="5">
                    <Button x:Name="btnFormat" Content="{DynamicResource BtnFormat}" Margin="0,4" Padding="8" Cursor="Hand" Click="btnFormat_Click" IsEnabled="False" />
                    <Button x:Name="btnAssign" Content="{DynamicResource BtnAssign}" Margin="0,4" Padding="8" Cursor="Hand" Click="btnAssign_Click" IsEnabled="False" />
                    <Button x:Name="btnDeletePartition" Content="{DynamicResource BtnDeletePart}" Margin="0,4" Padding="8" Background="#FFFFD6D6" Foreground="#FF900000" Cursor="Hand" Click="btnDeletePartition_Click" IsEnabled="False" />
                </StackPanel>
            </GroupBox>

            <ProgressBar x:Name="progBusy" Height="12" Margin="0,8,0,4" IsIndeterminate="True" Visibility="Hidden" />
            <TextBlock x:Name="txtStatus" Text="{DynamicResource StatusWaiting}" Foreground="#FF0056B3" TextWrapping="Wrap" FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>